{
  "//": "Intentionally empty for the monorepo-wide `pnpm typecheck` — Next generates its own `.next/types/*` references and a real per-project tsconfig is bootstrapped by `next dev` / `next build`. To typecheck the example properly, run `pnpm --filter web exec tsc --noEmit` after a first `next dev` boot.",
  "extends": "../../../../tsconfig.base.json",
  "files": [],
  "include": [
    ".next/types/**/*.ts"
  ],
  "exclude": [
    ".next",
    "node_modules",
    "dist"
  ],
  "compilerOptions": {
    "allowJs": true,
    "incremental": true,
    "jsx": "preserve",
    "plugins": [
      {
        "name": "next"
      }
    ]
  }
}
