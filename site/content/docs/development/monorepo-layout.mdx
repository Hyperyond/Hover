# Monorepo layout

Hover is a single pnpm workspace. `packages/*` ship to npm; `examples/*` are smoke targets.

## Packages

The surface is the VS Code extension; `@hover-dev/core` is the shared engine. (The old bundler-plugin / in-page-widget packages — `vite-plugin-hover`, `@hover-dev/astro` / `nuxt` / `next`, `webpack-plugin-hover`, `@hover-dev/cli`, `@hover-dev/widget-bootstrap`, `@hover-dev/transform-source` — have been removed; their integration notes live only in git history.)

| Package | What it does |
|---|---|
| `hover-dev` (`packages/vscode-ext`) | The VS Code extension. Chat webview + Dashboard / Conversations / Network / Environments / Settings views; ships the engine inside the `.vsix` (`scripts/stage-engine.mjs` `npm pack`s local `packages/core`) and spawns it under VS Code's own Node. Publisher `hyperyond`. |
| `@hover-dev/core` | The Node engine: agent CLI invocation, Playwright CDP preflight, WebSocket bridge between the client and the agent. Owns the plugin API. |
| `@hover-dev/api-test` / `@hover-dev/pentest` | 🟠 API testing (MITM HTTPS proxy + flow inspector + MCP) and 🔴 Pentest modes, built on the shared private `packages/probe-engine`. Never published (probe seeds inlined at build). See [Features → API & security testing](/docs/features/api-test/). |

## Examples

Each example runs on its own port. They are the testing-surface targets the extension drives over CDP:

| Example | Port | Stresses |
|---|---|---|
| `basic-app` | 5173 | Login + counter + todos — minimal happy-path test |
| `e-commerce` | 5174 | Cart state, product grid, checkout, cross-tab popup payment flow |
| `stock-registration` | 5175 | Realistic ~50-field brokerage account form, conditional reveals |
| `canvas-paint` | 5176 | Canvas drawing + DOM toolbar (canvas pixels are opaque to Playwright snapshots) |
| `payment-provider` | 5177 | Deliberately unintegrated third-party payment page; popup target for e-commerce |

The bundler-dogfood examples (`astro-app` / `nuxt-app` / `next-app` / `webpack-app` / `rn-web-app`) and `turbo-monorepo` were removed along with the bundler-plugin path — the remaining examples are single-URL state-machine SPAs the extension drives. None install anything Hover-related; they're plain Vite + React apps.

## `dist`-entry package (`@hover-dev/core`)

Most packages set `main` / `exports` directly to `src/*.ts`, so consumers' transpilers see TypeScript source with zero build step.

**The exception, `@hover-dev/core`,** points `main` / `exports` at `dist/*.js`. The reason is the VS Code extension's staged engine: `engine/host.mjs` imports `@hover-dev/core/service`, so core must build to `dist`.

Practical implications:

- A root `postinstall` hook builds core (plus the api-test / pentest plugins) after every `pnpm install`. Fresh clones get usable `dist/` artefacts immediately.
- Editing `core/src` during extension dev needs a `pnpm --filter @hover-dev/core build` (or a `dev` watch terminal) so the staged engine picks it up; `pnpm --filter hover-dev package` re-packs core fresh into the `.vsix`.
