# Save as Jira case

<Callout type="warning" title="Not exposed by the VS Code extension">

This output was part of the original in-page widget. The VS Code extension's **Save as** emits **only** the Playwright spec — the `.case.csv` writer still lives in `@hover-dev/core` but the extension chat does not surface it. The **[VS Code extension](/docs/get-started/install)** is the recommended way to use Hover.

</Callout>

Emit `__vibe_tests__/<slug>.case.csv` — an Xray-compatible multi-row CSV that imports straight into Jira / Xray / Zephyr Scale as a Manual Test issue.



Full content coming soon. See the README section [Three crystallisation formats](https://github.com/Hyperyond/Hover#what-you-get-today).


