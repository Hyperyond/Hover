# Reference

Lookup material for users who already know how Hover works.

- **[CLI (`hover run`)](/docs/reference/cli)** — terminal authoring with `hover run "<prompt>"`, plus the frozen `npx @hover-dev/cli setup` bundler wirer.
- **[Agent registry](/docs/reference/agent-registry)** — the `AgentDescriptor` interface and how to add a new agent.
- **[WebSocket protocol](/docs/reference/websocket-protocol)** — every `{ type, payload }` message the chat / widget and the engine exchange.
- **[Architecture](/docs/reference/architecture)** — the boundary constraints, sandboxing rationale, and the CDP-driven flow.
- **[Roadmap](/docs/reference/roadmap)** — what's shipped, what's in flight, what's planned.

### Bundler plugins (frozen)

The original in-page-widget path. Frozen — still installable and documented, but the [VS Code extension](/docs/get-started/install) is the going-forward surface.

- **[Plugin options](/docs/reference/plugin-options)** — every config flag on every bundler integration.
- **[Plugin API](/docs/reference/plugin-api)** — `defineHoverPlugin` + manifest shape for writing your own optional plugins (security mode is the first one).
