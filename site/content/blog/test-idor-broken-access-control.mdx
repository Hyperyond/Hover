---
title: "Test for IDOR and broken access control from your dev browser"
description: "Broken access control is the #1 web security risk and rarely gets an automated test. Point your agent at Hover and it replays your app's own API calls with mutated IDs and tokens, then crystallizes the probe as a Playwright spec."
date: 2026-06-07
author: "Hover"
tags: [security, idor, access-control]
---

# Test for IDOR and broken access control from your dev browser

Broken access control sits at the top of the OWASP Top 10, and your end-to-end suite rarely covers it. Your tests log in as the right user and check that the right things happen. They don't log in as user A and try to read user B's invoice. That second test catches an IDOR, and writing it usually means leaving the browser for Burp or a stack of curl commands.

## Why access-control bugs slip through

An IDOR (insecure direct object reference) is simple: the server trusts an ID in the request instead of checking who's asking. `GET /api/invoices/1043` returns your invoice. Change `1043` to `1044` and it returns someone else's. The frontend never renders that URL, so a click-driven test never tries it. You find the hole by taking a real request and tampering with it.

## Replay your own requests, mutated

Hover can route your debug browser through a local HTTPS man-in-the-middle proxy (built on [mockttp](https://github.com/httptoolkit/mockttp), no Python and no system CA install) that records every call your dev page makes. The real requests — carrying real session cookies — become material the agent can work with.

From there you point the agent at them: re-issue `GET /api/invoices/1043` with the next ID, drop the auth header, swap a tenant parameter. It checks whether the response leaks data it shouldn't — the probe a happy-path test never runs.

There's no separate tool to set up. The agent drives whatever dev server you point Hover at, over CDP.

## A finding becomes a regression test

When the agent confirms a hole, Hover crystallizes it into `__vibe_tests__/<slug>.api-test.spec.ts`: plain Playwright on the `request` fixture, one `test()` per check, recording the intent and the status code each one expects. It runs in CI with `npx playwright test`, no proxy and no agent, so the authz hole you found today stays closed.

[Read about pointing an offensive agent at your own app →](/blog/pentest-mode-attack-your-own-app/)
