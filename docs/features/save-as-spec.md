# Save as Spec

Crystallize a verified session into a standard `@playwright/test` file under `__vibe_tests__/`.

::: info This page is a placeholder
Full content coming soon. The mechanism today: click the dropdown on the Result card, choose **Save as Spec**, name the slug. Hover emits `__vibe_tests__/<slug>.spec.ts` using `getByRole / getByLabel / getByTestId` semantic selectors and a JSDoc header with plain-English Steps + Expected blocks.

For now, see the README section [Three crystallisation formats](https://github.com/Hyperyond/Hover#what-you-get-today) and the [`examples/basic-app/__vibe_tests__/login-and-counter.spec.ts`](https://github.com/Hyperyond/Hover/blob/main/examples/basic-app/__vibe_tests__/login-and-counter.spec.ts) sample output.
:::
