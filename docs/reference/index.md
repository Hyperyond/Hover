# Reference

Lookup material for users who already know how Hover works.

- **[Plugin options](./plugin-options)** — every config flag on every bundler integration.
- **[CLI](./cli)** — `npx @hover-dev/cli add` flags, framework forcing, dry-run, troubleshooting.
- **[Agent registry](./agent-registry)** — the `AgentDescriptor` interface and how to add a new agent.
- **[WebSocket protocol](./websocket-protocol)** — every `{ type, payload }` message the widget and service exchange.
- **[Architecture](./architecture)** — the boundary constraints, sandboxing rationale, and Phase 0 → Phase 3 development order.
- **[Roadmap](./roadmap)** — what's shipped, what's in flight, what's planned.
